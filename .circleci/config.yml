# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2
jobs:
  build:
    docker:
    - image: golang:1.13.1-alpine3.10
    working_directory: /go/src/github.com/smallinsky/mtf
    steps:
      - checkout
      - run: apk add make
      - run: go get -v -t -d ./...
      - run: make test
